classdef uart_port_type < Simulink.IntEnumType
    % uart_port_type Enumeration type definition for use with System Composer profile

    enumeration
        uart_tx(0)
        uart_rx(1)
    end

end
